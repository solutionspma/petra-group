[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"

# SPA fallback — Netlify still serves /.netlify/functions/* first; this documents intent.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "node build.js && python3 -m http.server 8000 --directory dist"
  port = 8000

# iOS Mail / configuration profiles must be served with Apple’s MIME type so Safari offers “Install”.
[[headers]]
  for = "/assets/documents/email-profiles/*.mobileconfig"
  [headers.values]
    Content-Type = "application/x-apple-aspen-config; charset=utf-8"
