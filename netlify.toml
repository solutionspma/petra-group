[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "node build.js && python3 -m http.server 8000 --directory dist"
  port = 8000
