# Document Depot — iOS “Install profile” overrides (profile-flags Netlify function)
# Import into Netlify:
#   netlify env:import PROFILE_FLAGS.ENV
# Or upload this file in Site configuration → Environment variables → Import from .env

EMAIL_IOS_LIVE_KEVIN_DICKERSON=1
